# Legitimate path exclusions for scripts/scan-secrets.mjs (p1-p2 08).
# One tracked path per line; `#` comments allowed. Path-only on purpose:
# content waivers would become a hiding place. Add here ONLY for planted
# fakes / illustrative key material — and keep the fixture file itself
# concatenation-built so NEW commits stay clean (docs/lessons.md 31).

# The scanner's own contract test plants full-length fake tokens in throwaway
# repos; its pre-concatenation commits (audit-fixes batch) carry literal
# fakes in history that rewriting cannot un-blob.
tests/plan/scan-secrets.test.ts
