test(e2e): local LLM guide, deflake tooling, and coverage completion (tickets 05-07)

Local real-LLM workflow (ticket 05)
- pnpm test:e2e:llm now fails fast with setup instructions when the key is
  missing (scripts/require-env.mjs) instead of silently skipping
- tests/e2e/README.md documents the layers, which ones run in CI, endpoint
  overrides (NOVA_E2E_KEY_ENV / _BASE_URL / _MODEL), cost and runtime,
  rate-limit behaviour, artifact locations, key rotation, and how to write
  a case that cannot pass for the wrong reason
- README links the guide

Stability tooling (ticket 06)
- scripts/deflake.mjs repeats a command N times and reports which tests are
  flaky versus failing every run (JSON reporter, exit code reflects failures)
- E2E artifacts now land in a per-run timestamp+pid directory and each case
  prints its path, so evidence is separable when reproducing with deflake

Coverage completion (ticket 07)
- deterministic (no key): --resume renders a hand-written session history
  from <NOVA_HOME>/.nova/sessions; /compact reports the no-op exactly once
  per invocation (regression guard for the duplicated notice)
- real LLM: long tool output folds with a hidden-line counter (seq 1 40 ->
  "more lines"); delegating via spawn_subagent surfaces the [subagent
  lifecycle notices, and the case skips explicitly when the model decides
  not to delegate (that is model behaviour, not a product bug)

Verified: 15 E2E tests green (4 print-mode, 5 deterministic, 6 interaction)
against the real endpoint, 3 deterministic cases green under CI=true, and
670 unit/component tests green.
