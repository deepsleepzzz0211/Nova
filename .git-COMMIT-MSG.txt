fix(tui): review fixes for tickets 09 + 14

Spec review findings:
- context gauge now uses the LAST request''s prompt tokens (new
  PromptCacheMetrics.lastInputTokens) instead of the monotonically
  growing session total, which saturated at 100% and made the warning
  color meaningless
- footer renders the window and strategy: ctx 12%/100.0k (compact)
- cost shows — before any usage is recorded (was $0.0000)
- /model switch keeps the model cost (SwitchResult/resolveSpec carry it,
  the footer no longer goes permanently blank for priced models)
- PermissionDialog routes keys through the FILTERED option list: on a
  dangerous call ''3'' no longer resolves always and Down-Down+Enter
  lands on the last visible option
- InputBar border color extracted as a tested pure function
  (workingBorderColor); git branch now resolves from subdirectories and
  worktrees (.git gitdir pointer)

Standards review findings:
- status-format stays pure: readGitBranch moved to tui/git-branch.ts;
  ModelCost imported from the catalog instead of being duplicated
- StatusBar takes CacheStatsView instead of an inline duplicate type
- parseToolArgs() helper replaces four hand-rolled JSON.parse blocks;
  unused file_path/filePath aliases dropped (Speculative Generality)
- InputBar dead duplicate Esc branch removed; ChatView doc corrected;
  MessageBubble thinking access simplified

Ticket 09 status recorded as done; lessons.md gains the 6th
heredoc-escape recurrence note. 596 tests.
